<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="computer.benjamin.zotdr0id.activity.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <TextView
        android:id="@+id/intro_help_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:contextClickable="false"
        android:elegantTextHeight="true"
        android:ems="10"
        android:focusedByDefault="false"
        android:inputType="textMultiLine"
        android:singleLine="false"
        android:layout_marginTop="@dimen/main_content_top_margin"
        android:text="@string/intro_help_text"
        android:textSize="16sp"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        tools:text="@string/intro_help_text" />

    <me.maxwin.view.XListView
        android:id="@+id/listViewMain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_marginTop="@dimen/main_content_top_margin"
        android:paddingTop="@dimen/nav_header_vertical_spacing" />

</RelativeLayout>
